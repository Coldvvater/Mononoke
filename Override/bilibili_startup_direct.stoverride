name: bilibili直连开启
desc: B站启动时开启直连模式
# 因常常观看港澳台番剧后，B站连着外网，下次启动时，需要让其直连模式（主界面不同），有此需求者可安装此覆写。
# 特别鸣谢：Cuttlefish 创作脚本
http:
  mitm:
    - "api.bilibili.com"
  script:
    - match: ^https?:\/\/api\.bilibili\.com\/x\/offline\/version
      name: biliSUD
      type: response
      require-body: true
      timeout: 10
script-providers:
  biliSUD:
    url: https://raw.githubusercontent.com/ddgksf2013/Cuttlefish/master/Script/bilibili_startup_direct.js
    interval: 86400
